import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mobiledesktop.template',
  appName: 'Mobile Desktop Template',
  webDir: 'www',
};

export default config;
